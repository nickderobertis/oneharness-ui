{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window receives only Tauri core window capabilities.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
